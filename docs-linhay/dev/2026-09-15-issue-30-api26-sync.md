# Issue #30：API 26 文档同步记录

日期：2026-09-15

## 结果

基于用户提供的 `inputs/DevEco-Studio.app` 完成 HarmonyOS SDK 26.0.0 Release 的离线声明快照导入。

- SDK：`26.0.0.105`
- API：`26.0.0 Release`
- 导入范围：OpenHarmony JS API、HMS JS API、HMS Kit 声明
- 导入文件：529 份 Markdown 声明快照
- 索引：重建 `references/INDEX.md` 与 `JsEtsAPIReference/INDEX.md`
- 工具：新增 `scripts/sync_api26_snapshot.py`，可从同类 DevEco SDK 重复生成

## 边界

SDK 声明提供 API 名称、签名和类型，不能替代官网的完整中文说明、示例、变更说明和兼容性段落。官网 API Change List 与升级指南仍作为行为变化和适配判断的权威来源。

## 验证

已通过 `check_packaging_docs.py`、`reference_compat.py check`、API 26 同步工具自检和 90 项 Python 单元测试。

