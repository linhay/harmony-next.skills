# 2026-05-04

## issue 9

- 补齐 `JsEtsAPIReference/guides/` 下 3 个状态管理迁移 guide，本地覆盖 `arkts-v1-v2-migration-inner-object`、`arkts-v1-v2-mixusage`、`arkts-new-makeobserved`。
- `reference_compat.py generate` 现在会重写上述 guide 外链并刷新索引。
- `reference_compat.py check` 现在会把这 3 类目标 guide 外链残留视为失败。
- 已根据官方 `arkts-v1-v2-guide` 主线补齐本地 guide 的章节结构，并新增对新版 slug 的离线映射支持。
- 已继续清理同类状态管理外链，覆盖 `状态管理概述`、`@Observed/@ObjectLink`、`@ObservedV2/@Trace`、`addMonitor/clearMonitor`、`applySync/flushUpdates/flushUIUpdates`、`AppStorageV2`、`PersistenceV2`、`@Env`。
- 已继续收敛状态管理 / ArkUI 基础语义外链，覆盖 `arkts-watch`、`arkts-new-monitor`、`arkts-appstorage`、`arkts-localstorage`、`arkts-persiststorage`、`arkts-create-custom-components`、`arkts-page-custom-components-lifecycle`。
- 已将 `arkts-state`、`arkts-state-management-glossary`、`arkts-provide-and-consume` 统一离线到本地 `@ohos.arkui.StateManagement (状态管理)` 模块页。
- `reference_compat.py` 现支持按 slug 丢弃原在线锚点，避免本地重写后留下无效锚点。
- 当前明确暂不批量替换 `arkts-two-way-sync`、`arkts-new-binding`、`arkts-builder`，原因是仓库内还缺对应的一页式离线总览页。

- `harmony-next` 的 issue 9 离线化继续扩展：`reference_compat.py` 新增 `arkts-global-interface -> types/classes/Class (UIContext).md` 与 `arkts-routing -> types/classes/Class (Router).md` 两条映射，统一丢弃在线锚点；`generate` 后实际重写 26 个 guide-link 文件，`check` 通过。
- 继续保守维持“只改已有明确离线落点”的策略；`arkts-rendering-control-ifelse`、`arkts-sendable`、`arkts-two-way-sync`、`arkts-new-binding`、`arkts-builder`、`arkts-reusable` 仍暂不替换，因为当前仓库没有语义等价的一页式离线总览页。
- 又补了 3 个明确漏修的在线 guide：`arkts-common-components-text-input -> TextInput.md`、`arkts-common-events-focus-event -> 焦点事件.md`、`pastebutton -> PasteButton.md`；`generate` 后实际新增重写 8 个 guide-link 文件。
- 对剩余高频项做了跨目录审计；`module-configuration-file`、`app-startup`、`uiability-usage` 等目前只能命中零散 API/快速开始页，仍没有语义等价的一页式离线 guide，因此先停止继续替换。
- 策略已升级为“没有语义等价本地页时，直接抓官方 guide 正文并落成本地离线版”；本轮新补 `window-terminology`、`uiability-usage`、`module-configuration-file`、`app-startup`、`arkts-sendable`、`arkts-builder`、`arkts-two-way-sync`、`arkts-new-binding`、`arkts-rendering-control-ifelse` 9 个 guide。
- 已验证官方接口 `https://developer.huawei.com/consumer/cn/documentPortal/getDocumentById` 支持按 `objectId=<slug>` 拉取 guide HTML 正文，适合作为后续离线化补文入口。
- 本轮 `reference_compat.py generate` 实际重写 88 个 guide-link 文件，随后 `check` 通过。
