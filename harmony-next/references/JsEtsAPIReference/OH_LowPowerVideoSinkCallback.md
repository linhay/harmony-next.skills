# OH_LowPowerVideoSinkCallback

```ets
typedef struct OH_LowPowerVideoSinkCallback OH_LowPowerVideoSinkCallback
```

#### 概述

包含了LowPowerVideoSink回调函数指针的集合。

 应用需注册此实例结构体到[OH_LowPowerVideoSink](OH_LowPowerVideoSink.md)实例中，并对回调上报的信息进行处理，保证LowPowerVideoSink的正常运行。

**起始版本：** 20

**相关模块：**[LowPowerVideoSink](LowPowerVideoSink.md)

**所在头文件：**[lowpower_video_sink_base.h](lowpower_video_sink_base.h.md)