# Camera_Point

```ets
typedef struct Camera_Point {...} Camera_Point
```

#### 概述

点参数。

**起始版本：** 11

**相关模块：**[OH_Camera](OH_Camera.md)

**所在头文件：**[camera.h](../../capi/headers/camera.h.md)

#### 汇总

#### 成员变量

名称描述double xX坐标，取值范围为[0,1]。double yY坐标，取值范围为[0,1]。