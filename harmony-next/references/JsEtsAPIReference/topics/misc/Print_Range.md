# Print_Range

```ets
typedef struct {...} Print_Range
```

#### 概述

打印范围。

**起始版本：** 13

**相关模块：**[OH_Print](OH_Print.md)

**所在头文件：**[ohprint.h](../../capi/headers/ohprint.h.md)

#### 汇总

#### 成员变量

名称描述uint32_t startPage打印起始页。uint32_t endPage打印终止页。uint32_t pagesArrayLen打印队列长度。uint32_t* pagesArray打印队列指针。